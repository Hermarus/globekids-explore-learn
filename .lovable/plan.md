
# Изменение направления: Нячанг -> Фукуок

## Что будет изменено

В файле `src/components/DestinationsSection.tsx` в карточке Вьетнама:

- **Город**: "Нячанг" -> "Фукуок"
- **Описание**: обновить текст с учётом Фукуока (тропический остров)
- **Возраст/группа**: оставить "12-15 детей" (размер группы), возраст 8-15 лет упоминается в других секциях сайта
- **Температура**: "+28°C" -> "+30°C" (Фукуок чуть теплее)
- **Фичи**: обновить теги (например, "Остров" вместо "Резорт")

Изображение останется тем же (`vietnam.jpg`), так как оно подходит по стилю тропического направления.

### Технические детали
Изменения только в одном файле: `src/components/DestinationsSection.tsx` -- обновление объекта данных для Вьетнама в массиве `destinations`.
