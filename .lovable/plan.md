

## Изменение получателя заявок в Telegram

Сейчас заявки с сайта отправляются в Telegram через бот, но на другой chat ID. Нужно обновить значение секрета `TELEGRAM_CHAT_ID` на новый ID пользователя @LingvoCamp2016.

### Что будет сделано

Обновлю секрет `TELEGRAM_CHAT_ID` на значение `6386058031`, чтобы все заявки приходили именно этому пользователю.

### Важное условие

Бот должен быть запущен пользователем @LingvoCamp2016 -- то есть этот человек должен отправить боту команду `/start`, чтобы бот мог отправлять ему сообщения. Без этого Telegram API вернёт ошибку "chat not found".

### Технические детали

- Обновление одного секрета `TELEGRAM_CHAT_ID` через инструмент управления секретами
- Код edge-функции `send-telegram` менять не нужно -- она уже читает `TELEGRAM_CHAT_ID` из переменных окружения

